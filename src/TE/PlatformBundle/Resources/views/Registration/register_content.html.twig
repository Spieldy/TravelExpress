{% trans_default_domain 'FOSUserBundle' %}

<form class="CenteredForm FormInscription" action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
    {{ form_widget(form) }}
    <div>
        <input class="Btn" type="submit" value="Inscription" />
    </div>
</form>
